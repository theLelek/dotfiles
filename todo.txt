adapt whole rice to rose pine color scheme

setup.sh doesnt properly work for changing default terminal emulator

kitty:
- change look of tabs (maybe use custom function)
- blur on backround doesnt work (probably an actuall bug) 
- in bottom right corner add github audio visualizer

